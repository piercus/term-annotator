{
  "name": "term-annotator",
  "version": "0.0.3",
  "description": "annotator",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=development electron index.js --disable-gpu",
    "startp": "cross-env NODE_ENV=production electron index.js",
    "lint": "xo --env=browser",
    "package-windows": "electron-packager . --overwrite --platform=win32 --arch=x64 --ignore=^/bin && mkdir ./term-annotator-win32-x64/bin && cp ./bin/ffmpeg.exe ./term-annotator-win32-x64/bin/ffmpeg.exe && zip -r term-annotator-app-windows.zip term-annotator-win32-x64",
    "package-linux": "electron-packager . --overwrite --platform=linux --arch=x64 --ignore=^/bin && mkdir ./term-annotator-linux-x64/bin && cp  ./bin/ffmpeg ./term-annotator-linux-x64/bin/ffmpeg && zip -r term-annotator-app-linux.zip term-annotator-linux-x64",
    "semantic-release": "semantic-release"
  },
  "author": "Someone",
  "license": "MIT",
  "devDependencies": {
    "@semantic-release/github": "^5.2.10",
    "cross-env": "^5.1.3",
    "electron": "^6.0.0",
    "electron-packager": "^13.1.1",
    "electron-store": "^2.0.0",
    "mkdirp": "^0.5.1",
    "semantic-release": "^15.13.12"
  },
  "build": {
    "appId": "my.id",
    "mac": {
      "category": "your.app.category.type"
    }
  },
  "dependencies": {
    "ejs": "^2.6.1",
    "electron-store": "^2.0.0",
    "fluent-ffmpeg": "^2.1.2",
    "gif-frames": "^1.0.1",
    "mv": "^2.1.1",
    "xo": "^0.24.0"
  },
  "release": {
    "branch": "tan/9-ident-ability",
    "plugins": [
      [
        "@semantic-release/github",
        {
          "assets": [
            {
              "path": "term-annotator-app-windows.zip",
              "label": "term-annotator-app-windows"
            },
            {
              "path": "term-annotator-app-linux.zip",
              "label": "term-annotator-app-linux"
            }
          ]
        }
      ]
    ]
  }
}
